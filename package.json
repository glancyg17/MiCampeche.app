{
  "name": "micampeche-app",
  "private": true,
  "description": "Static PWA frontend for MiCampeche. No build step. package.json exists only to hold test dependencies for test-smoke.js.",
  "devDependencies": {
    "canvas": "^3.2.3",
    "jsdom": "^27.4.0"
  },
  "scripts": {
    "stamp": "node scripts/stamp.js",
    "test": "node scripts/stamp.js --check && node test-sw.js && node test-selfheal.js && node test-smoke.js"
  }
}
